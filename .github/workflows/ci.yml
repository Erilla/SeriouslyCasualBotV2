name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
  # Allow deploy.yml to reuse this workflow as its CI gate. Without this
  # trigger, `uses: ./.github/workflows/ci.yml` fails at startup ("workflow
  # file issue"), which is why every Deploy run failed in 0s.
  workflow_call:

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: Type check
        run: npm run typecheck

      - name: Unit tests
        # Stub required env vars so config.ts module load succeeds. These
        # are only used if a test actually hits a code path that reads them;
        # the values are intentionally unusable so any accidental production
        # call fails loudly.
        env:
          DISCORD_TOKEN: test-stub
          CLIENT_ID: test-stub
          GUILD_ID: test-stub
          OFFICER_ROLE_ID: test-stub
          WOWAUDIT_API_SECRET: test-stub
          WARCRAFTLOGS_CLIENT_ID: test-stub
          WARCRAFTLOGS_CLIENT_SECRET: test-stub
          WARCRAFTLOGS_GUILD_ID: '0'
          RAIDERIO_GUILD_IDS: '0'
        run: npm test

      - name: Build
        run: npm run build
